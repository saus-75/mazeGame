
public interface Navigator {
	/**
	 * Changes the mode, in which changes to the corrsponding panel
	 * @param newMode is a char which is linked to a panel
	 */
	public void changeMode(char newMode);
}
